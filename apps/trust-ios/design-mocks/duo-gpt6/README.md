# Archived interactive prototype

This standalone prototype is retained as a historical artifact. It is not a current product specification and must not guide implementation. Use [docs/DESIGN.md](../../../../docs/DESIGN.md) for current visual direction and screen intent.

Open `index.html` in a browser to inspect the prototype. Its screens, interactions, copy, layout, and visual system may differ from the app.
