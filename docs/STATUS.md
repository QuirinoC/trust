# Trust project status

Updated 2026-09-26. This is a dated evidence snapshot, not a live dashboard.

## Current changes and evidence

- The handle-based connection-request milestone is merged in PR [#6](https://github.com/QuirinoC/trust/pull/6), merge commit `5642f808bc589772cebb7625cb7e9b55488bcbb0`. A person can find an exact handle and send a request. The recipient accepts or declines; both sharing directions start Off. Request lists show incoming and sent requests. Verified phone ownership is required for connection discovery and requests. The phone-verification skip is limited to the internal preview build and does not satisfy the server's connection eligibility check.
- API tests passed **125/125**. Swift package tests passed **29/29**. Full Duo and stable iPhone 17 Pro simulator runs each passed **37/37** with no skips. The real-API onboarding, request, acceptance with both shares Off, and stop-sharing test passed after review fixes. The focused appearance persistence test passed **1/1** on iPhone 17 Pro with iOS 26.5 and no skips. PR CI passed API, iOS, and web jobs. Website tests passed **3/3**. Localization checks passed for **459 keys across 5 translated locales**.
- Luna implemented the change; Sol reviewed and rechecked account isolation, phone-verification return navigation, and legacy-invite request cleanup. Astra reviewed the five light-mode Duo screenshots and the final iPhone result, incoming-request, and sharing-Off screenshots without blockers. Screenshots are saved locally under the ignored `test-results/sharing-2026-09-26/` directory. Dark appearance has interaction-test coverage; large text and dark-mode screenshots were not separately reviewed.
- iOS source is build **29**. Its stable-Xcode internal archive was created successfully and is ready, but it has **not been uploaded**. Export stopped with Xcode exit code 70, “Failed to Use Accounts”: Apple team `3S529795M9` needs renewed App Store Connect access. The Mac is locked, so account renewal is pending. Build 29 processing and tester-group assignment are unverified because it has not been uploaded. Build **28** uploaded successfully but contains superseded UX and is not the intended replacement; its internal-group assignment is unverified.
- Render API deployment `dep-darphqbncjis73dse36g` is live at merge commit `5642f808bc589772cebb7625cb7e9b55488bcbb0`. `/health/live` and `/health/ready` returned `Healthy`; unauthenticated `/api/v1/people/lookup` and `/api/v1/connection-requests` returned **401**, not 404.
- The website privacy and invite updates are live in Worker version `1c4e9f11-813b-4b91-bc47-8a665b781459`. `/`, `/privacy`, `/terms`, `/support`, `/sms`, and the Apple App Site Association file returned 200. The valid legacy invite route `/i/ABC234` returned 200 with Open Trust and explicit acceptance; the manual-code fallback has been removed. Website tests passed **3/3** after the change.
- No end-to-end push notification delivery has been verified. Do not claim that notifications are working; the app cannot guarantee delivery.
- English is the source locale, with Simplified Chinese, Japanese, German, French, and Brazilian Portuguese overlays. Website legal pages and App Store metadata remain English.

The current release design and screen intent live in [DESIGN.md](DESIGN.md). Broader production gaps remain tracked in [PRODUCTION-READINESS.md](PRODUCTION-READINESS.md). See [FEATURE-E2E-PLAN.md](FEATURE-E2E-PLAN.md) for test evidence and [DEPLOYMENT.md](DEPLOYMENT.md) for release procedures.
